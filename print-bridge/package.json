{
  "name": "pos-print-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone LAN bridge that turns POST /print requests into raw ESC/POS bytes sent to a thermal printer.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
